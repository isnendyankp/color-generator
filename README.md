# Description
```

This project color-generates a random color and displays it on the screen. It also displays the color's hex code and RGB values. The user can click a button to generate a new color. The user can also click a button to copy the color's hex code to the clipboard.